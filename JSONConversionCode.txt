#JSONConversionCode

import re

import json

import pprint

filename = '../data/12054.txt'

with open(filename, 'r') as f:
    for lines in f:
        file = f.readlines()
        
        dictionary = dict(re.findall(r'(.*):( .*)', str(file))) #.* matches all characters
        
        json = json.dumps(dict(re.findall(r'(.*):( .*)', str(file))))
        
        #str() to convert the file to a string
        #()\ matches characters in ()
        #json.dumps function to group similar keys together
        #pprint.pprint function to print key: value pairs on new line
        #pprint.PrettyPrinter function to print each entry to a new line as dictionary     width is too short when printed

    #print(dictionary)
    #pprint.pprint(dictionary)
    #print(json)
    #pprint.pprint(json)
    better = pprint.PrettyPrinter(width=200)#adjust width argument accordingly
    better.pprint(dictionary)
    #better.pprint(json)
    